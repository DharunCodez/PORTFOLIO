"# PORTFOLIO

## Deploying on Windows

If npm fails with a PowerShell execution policy error, run:

```powershell
Set-ExecutionPolicy -Scope CurrentUser RemoteSigned -Force
```

Or use the Windows batch script:

```bat
deploy.bat
```

This project builds with:

```bash
npm install
npm run build
```" 
